import 'package:flutter/material.dart';

class LoadinngScreen extends StatelessWidget {
  const LoadinngScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('LoadinngScreen')),
      body: const Center(child: Text('LoadinngScreen')),
    );
  }
}
